<%= simple_form_for @article do |f| %>
  <%= f.input :title, label: 'Title', error: 'Please enter a title for the article' %>
  <%= f.input :content %>
  <%= f.button :submit, class: 'btn btn-light border-dark' %>
<% end %>
